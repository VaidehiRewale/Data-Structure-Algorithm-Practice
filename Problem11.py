Implement a function that checks if a given string is a palindrome. A palindrome is a word, or a sequence of characters that reads the same forward and backward. 

String = input("Enter a string:")
if String == String [::-1]:
    print(f"{String} is a palindrome:")
else:
    print(f"{String} is not a palindrome:")

